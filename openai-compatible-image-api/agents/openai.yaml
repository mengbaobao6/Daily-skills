interface:
  display_name: "OpenAI Compatible Image API"
  short_description: "Generate and edit images via compatible Images API."
  default_prompt: "Use $openai-compatible-image-api to generate or edit an image with the configured third-party Images API."

policy:
  allow_implicit_invocation: true
